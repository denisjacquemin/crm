<!DOCTYPE html>
<html>

    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Milo</title>
        <meta name="csrf-token" content="{{csrfToken}}">
        <link rel="stylesheet" href="/public/css/styles.css" />
        <script type="text/javascript" src="/public/bundle.js" defer ></script>
        <script type="text/javascript" src="/public/js/vendor/alpine/alpine.3.10.5.min.js" defer></script>
    </head>

    <body class="" x-data="{ navState: 'menu' }">
        <div class="h-screen flex flex-col">
        {{!-- <div x-text="navState" class="absolute top-0 right-0 z-50 text-red-600"></div> --}}
            {{> nav }}  
            {{#if messages}} 
                {{messages}}  
            {{/if}}
            {{{body}}}
        </div>
        {{> notifications}}
    </body>

</html>